// sentencia for of

let frutas = ["manzana", "pera", "uva"]; // Array

// Recorre directamente los valores del array
for (let fruta of frutas) {
  console.log("Fruta:", fruta); // Muestra cada fruta
}