// bucle for

// for (inicio; condición; incremento)
for (let i = 0; i < 5; i++) {
    // let i = 0 → inicia en 0
    // i < 5 → se repite mientras sea menor que 5
    // i++ → aumenta en 1 cada vuelta
  
    console.log("Valor de i:", i); // Muestra el valor actual
  }